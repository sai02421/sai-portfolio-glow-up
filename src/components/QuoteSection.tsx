
const QuoteSection = () => {
  return (
    <section className="py-8 md:py-12 animate-fade-in" style={{ backgroundColor: '#1C2533' }}>
      <div className="max-w-5xl mx-auto px-6">
        <blockquote className="text-2xl md:text-4xl font-medium italic text-center" style={{ color: '#E2E8F0' }}>
          "I build with empathy, validate with data, and launch for impact."
        </blockquote>
      </div>
    </section>
  );
};

export default QuoteSection;
