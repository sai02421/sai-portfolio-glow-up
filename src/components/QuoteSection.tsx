
const QuoteSection = () => {
  return (
    <section className="bg-soft-black py-16 md:py-20 animate-fade-in">
      <div className="max-w-5xl mx-auto px-6">
        <blockquote className="text-2xl md:text-4xl font-medium italic text-center" style={{ color: '#D4C5B0' }}>
          "I build with empathy, validate with data, and launch for impact."
        </blockquote>
      </div>
    </section>
  );
};

export default QuoteSection;
